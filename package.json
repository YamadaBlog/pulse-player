{
  "name": "pulse-player",
  "version": "0.13.0",
  "private": true,
  "description": "Reusable Vue 3 floating + inline music player components with persistent global audio state, draggable FAB, progress ring and FFT equalizer.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "type-check": "vue-tsc --noEmit"
  },
  "dependencies": {
    "lucide-vue-next": "^0.300.0",
    "pinia": "^2.1.0",
    "vue": "^3.4.0"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.0",
    "typescript": "^5.4.0",
    "vite": "^5.0.0",
    "vue-tsc": "^2.0.0"
  }
}
