{
  "name": "@pulse/demo-svelte",
  "version": "0.0.0",
  "private": true,
  "description": "Svelte 5 demo of @pulse/svelte — shows <pulse-player> + <pulse-fab> Custom Elements + usePulseAudio() store contract in a real Svelte app.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@pulse/svelte": "*",
    "svelte": "^5.0.0"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^4.0.0",
    "typescript": "^6.0.3",
    "vite": "^8.0.16"
  }
}
