dist
node_modules
coverage
*.min.*
public/audio
docs/screenshots
# packages/ and apps/ workspaces ship their own formatting story
# (each one gets its own .prettierrc when promoted out of scaffold).
packages
apps
# Round-14 — pre-rendered player-shell captures (binary WebP + generated manifest)
src/assets/shells
